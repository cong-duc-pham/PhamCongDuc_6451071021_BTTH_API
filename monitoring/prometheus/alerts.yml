groups:
  - name: realtime-pipeline
    rules:
      - alert: DeadLetterQueueHasMessages
        expr: dead_letter_total > 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "Kafka dead_letter co message moi"
          description: "Co message xu ly that bai qua so lan retry va da vao dead_letter."
